<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.mapper.PmsStoreSkuStockExtMapper">
  
  <resultMap id="BaseResultMap" type="com.macro.mall.model.PmsStoreSkuStock">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="store_id" jdbcType="BIGINT" property="storeId" />
    <result column="product_id" jdbcType="BIGINT" property="productId" />
    <result column="sku_id" jdbcType="BIGINT" property="skuId" />
    <result column="sku_code" jdbcType="VARCHAR" property="skuCode" />
    <result column="stock" jdbcType="INTEGER" property="stock" />
    <result column="low_stock" jdbcType="INTEGER" property="lowStock" />
    <result column="locked_stock" jdbcType="INTEGER" property="lockedStock" />
    <result column="sale_count" jdbcType="INTEGER" property="saleCount" />
    <result column="status" jdbcType="INTEGER" property="status" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>

  <select id="selectByStoreIdAndSkuId" resultMap="BaseResultMap">
    select id, store_id, product_id, sku_id, sku_code, stock, low_stock, locked_stock, 
           sale_count, status, create_time, update_time
    from pms_store_sku_stock
    where store_id = #{storeId,jdbcType=BIGINT} and sku_id = #{skuId,jdbcType=BIGINT}
  </select>

  <select id="selectBySkuId" resultMap="BaseResultMap">
    select id, store_id, product_id, sku_id, sku_code, stock, low_stock, locked_stock, 
           sale_count, status, create_time, update_time
    from pms_store_sku_stock
    where sku_id = #{skuId,jdbcType=BIGINT}
    order by store_id asc
  </select>

</mapper>
